<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        
        <title>{{title}}</title>

        <link rel="stylesheet" type="text/css" href="http://cdn.ink.sapo.pt/2.2.0/css/ink-min.css" type="text/css" media="screen" charset="utf-8">

        <style type="text/css">
            h1, h2, h3 {
                margin: 0;
            }

            a {
                color: #333;
            }

            label {
                font-weight: bold;
                display: inline-block;
                margin-right: 1em;
            }

            ul {
                list-style-type: none;
                padding: 0;
            }

            pre, code {
                font-family: monospace;
            }

            .tag {
                background-color: #555;
                color: #FFF;
                padding: 0.125em 0.25em;
                border-radius: 0.25em;
                margin-right: 0.25em;
            }

            .return pre, .params pre, .box-attribute pre {
                display: inline-block;
                min-width: 14em;
                text-align: right;
                margin: 0;
            }

            .return label, .params label {
                min-width: 12em;
            }

            .example {
                background-color: #EEEEDD;
                margin: 0;
            }

            .description > * {
                display: inline-block;
            }

            .box-modules {
                position: fixed;
                right: 0;
                top: 0;
                font-size: 0.7em !important;
                background: rgba(255, 255, 255, 0.75);
            }

            .box-modules h2 {
                font-size: 1.2em !important;
            }

            .box-modules ul {
                margin: 0;
            }

            .box-modules a {
                text-decoration: none;
            }

            .box {
                border-top-left-radius: 1em;
                padding: 1em;
                margin-bottom: 0.5em;
                font-size: 0.9em;
            }


            .box-module, .box-class, .box-function, .box-attribute {
                border-top-width: 8px;
                border-top-style: solid;

                border-left-width: 8px;
                border-left-style: solid;
            }

            .box-module {    border-color: #AAA; }
            .box-class {     border-color: #C99; }
            .box-function {  border-color: #99C; }
            .box-attribute { border-color: #9C9; }
        }
        </style>
    </head>

    <body>
        {{#if modules.length}}
            <div class="box box-modules">

                <h2>Modules:</h2>
                <ul>
                {{#each modules}}
                    <li><a href="#{{{hash}}}">{{name}}</a></li>
                {{/each}}
                </ul>

            </div>
        {{/if}}

        {{> module}}

        {{#each modules}}
            {{> module}}
        {{/each}}

        </div>
    </body>
</html>
